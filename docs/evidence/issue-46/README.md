# Issue 46: real two-plate slicer acceptance

September 17, 2026. Source fixtures generated by `scripts/generate-print-fixtures.ts`.
Bambu Studio 02.08.01.55 imported/re-exported both scales through its installed CLI
with `--arrange 0 --orient 0`. OrcaSlicer 01.09.05.51 opened both actual source
projects through its installed GUI and saved them with Save Project As. Both
Parts and Hardware names were visibly present at each scale. Attached plate images
are Orca's own rendered thumbnails extracted from its 1:10 saved project.

The two attached audit logs compare each source to both real re-exports using
`scripts/verify-print-export.py`. Both scales retain 14 Parts objects and 10
Hardware objects (14 original physical identities), all 83 volumes, 311,988
triangles and six colors. All geometry remains inside its assigned 256 mm plate;
plate membership, positions and dimensions survive re-export within 0.01 mm.
Topology audit checks closed directed edges and nonzero areas, and slicer repair
counters remain zero. SHA256 hashes identify the six local artifacts; large 3MF
files remain in the worktree's ignored `.verification` directory.

Orca's CLI crashes when generating a named-plate texture without an OpenGL context
(`GLTexture::generate_from_text` → `PartPlate::generate_plate_name_texture` in the
macOS crash report). GUI verification preserves the actual plate names and avoids
that CLI-only defect. Bambu GUI automation timed out; its actual CLI project
reader/writer passed. No G-code slicing or physical printing is claimed. The
placeholder profile intentionally requires choosing a real printing profile.

Browser session `gym-wave2-print_plates`, port 5315: real module workers completed
1:10 and 1:20 exports (14 Parts /10 Hardware each); a terminated worker emitted no
callback. ExportMenu visual controls and interaction checks belong to #44.

After merging logo PR #41 from main, the actual custom nameplate test passes at
both denominators: volume equals source CAD / scale³, differs from stock lettering,
and remains one connected watertight shell. The 1:20 logo fixture also passes an
actual Bambu import/re-export audit (5 objects, 12 volumes, 117,894 triangles).
A further Orca GUI attempt for that custom fixture could not acquire a native
window; Orca's complete two-scale rack round trips above remain verified. The
custom logo's scale/CSG preservation is verified directly through exported meshes.
