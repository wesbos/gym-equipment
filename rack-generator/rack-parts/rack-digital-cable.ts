/** VOLTRA accessories and rack-mounted cable systems (#136). Metadata only (main bundle): never import Manifold builders here.
 * Family slot: list this file's entries in PARTS (`defineRackPart({ section: 'Digital & cable', ... })`); rack-registry.ts already spreads it. */
import type { RackPart } from '../rack-part.ts';
export const PARTS = [] as const satisfies readonly RackPart[];
