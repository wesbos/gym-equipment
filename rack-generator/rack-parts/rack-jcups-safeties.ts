/** Brand J-cups, safeties, spotter arms and pull-up bars (#133). Metadata only (main bundle): never import Manifold builders here.
 * Family slot: list this file's entries in PARTS (`defineRackPart({ section: 'J-cups & safeties', ... })`); rack-registry.ts already spreads it. */
import type { RackPart } from '../rack-part.ts';
export const PARTS = [] as const satisfies readonly RackPart[];
