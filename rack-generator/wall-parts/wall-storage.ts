/** Wall-mounted barbell, plate and accessory storage. Metadata only (main bundle): never import Manifold builders here.
 * Family slot: list this file's entries in PARTS; wall-registry.ts already spreads it. */
import type { WallPart } from '../wall-part.ts';
export const PARTS = [] as const satisfies readonly WallPart[];
